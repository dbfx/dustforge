appId: com.dustforge.app
productName: DustForge
copyright: Copyright © 2024 DustForge Contributors

directories:
  buildResources: resources
  output: dist

files:
  - out/**/*

extraResources:
  - from: resources/icon.ico
    to: icon.ico
    filter:
      - "**/*"
  - from: resources/icon.png
    to: icon.png
    filter:
      - "**/*"

win:
  target:
    - target: nsis
      arch:
        - x64
  icon: resources/icon.ico
  requestedExecutionLevel: requireAdministrator

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  deleteAppDataOnUninstall: false
  shortcutName: DustForge
  artifactName: DustForge-Setup-${version}.${ext}

mac:
  target:
    - target: dmg
      arch:
        - x64
        - arm64
  icon: resources/icon.png
  category: public.app-categories.utilities
  hardenedRuntime: true
  entitlements: resources/entitlements.mac.plist
  entitlementsInherit: resources/entitlements.mac.plist
  artifactName: DustForge-${version}-${arch}.${ext}

linux:
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64
  icon: resources/icons/
  category: Utility
  artifactName: DustForge-${version}-${arch}.${ext}

publish:
  provider: github
  releaseType: release
